<%= simple_form_for([:admin, item], :html=> { class: 'longf' }) do |f| %>
	<%= f.input :title, label: 'Заголовок' %>
	<%= f.input :description, as: :bootsy, label: 'Описание', :input_html => {:rows => 15} %>
	<%= f.button :submit, class: "btn-primary" %>
<% end %>