<blockquote class="comment">
  <%= simple_format(comment.text) %>
  <footer>
    <%= time_ago_in_words(comment.created_at) %> назад
    пользователем <cite><%= comment.author %></cite>
  </footer>
</blockquote>